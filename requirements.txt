# Core Flask dependencies
flask==3.0.0
flask-cors==4.0.0
gunicorn==22.0.0
werkzeug==3.0.1

# AI and ML - OpenAI
openai==1.12.0
tiktoken==0.6.0

# Database - Supabase
supabase==2.4.4
psycopg2-binary==2.9.9

# File processing libraries
PyPDF2==3.0.1
python-docx==1.1.0
pandas==2.2.0

# Text processing and encoding
chardet==5.2.0
langchain-text-splitters==0.0.1

# Utilities
python-dotenv==1.0.1
requests==2.31.0

# Enhanced processing
numpy==1.24.3
asyncio-throttle==1.0.2

# Machine Learning for clustering and similarity (basic versions)
scikit-learn==1.3.0
scipy==1.11.1

# Optional Advanced Neural Processing (deployment-friendly versions)
# These will be installed if available, but won't break deployment if they fail
sentence-transformers==2.2.2
transformers==4.30.0
textstat==0.7.3
networkx==3.1

# Note: Advanced packages like torch, faiss, hdbscan, umap-learn, and spacy
# are now optional and handled with fallbacks in the code
# This ensures deployment works even without these packages